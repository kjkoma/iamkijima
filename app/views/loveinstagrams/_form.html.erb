<%= form_for(@loveinstagram) do |f| %>
  <% if @loveinstagram.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@loveinstagram.errors.count, "error") %> prohibited this loveinstagram from being saved:</h2>

      <ul>
      <% @loveinstagram.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :loveword %><br>
    <%= f.text_field :loveword %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
