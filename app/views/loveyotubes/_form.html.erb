<%= form_for(@loveyotube) do |f| %>
  <% if @loveyotube.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@loveyotube.errors.count, "error") %> prohibited this loveyotube from being saved:</h2>

      <ul>
      <% @loveyotube.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :loveword %><br>
    <%= f.text_field :loveword %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
